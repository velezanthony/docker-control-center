# ==============================================================================
#  English message catalog.
#
#  This is the SOURCE language: every key must exist here. Other catalogs may
#  be incomplete — t() falls back to the key name, so a missing translation is
#  visible but never fatal.
#
#  Keys used inside pad()/rpad() must stay PLAIN TEXT: those helpers align by
#  character count and ANSI escapes would be counted as visible characters.
#  Everything else may embed the colour variables ($B, $D, $CY…), which are
#  already defined by the time this file is sourced.
#
#  To add a language: copy this file to i18n/<code>.sh, translate the values,
#  and teach _dcc_resolve_lang() in scripts/i18n.sh how to detect it.
# ==============================================================================

MSG+=(
	# --- Language selector ----------------------------------------------------
	# lang_name se lee del fichero SIN hacer source (ver name_of en lang.sh),
	# así que debe quedarse en UNA línea con el valor entre comillas dobles.
	[lang_name]="English"
	[lang_current]="Current language: %s %s"
	[lang_available]="Available:"
	[lang_menu_prompt]="Choose a language:"
	[lang_auto_desc]="detect it from the system"
	[lang_from_env]="(from DCC_LANG in your environment)"
	[lang_from_config]="(from %s)"
	[lang_from_locale]="(detected from your system locale)"
	[lang_from_default]="(default)"
	[lang_set_ok]="Language set to %s."
	[lang_save_failed]="Could NOT write %s — your preference was not saved."
	[lang_set_where]="Saved in %s"
	[lang_set_unknown]="Unknown language: '%s'."
	[lang_unset_ok]="Preference removed. Back to auto-detection."
	[lang_env_blocks]="This has NOT taken effect yet: you have DCC_LANG exported in this terminal, and the environment wins over the file."
	[lang_env_fix]="Run this and you are done:"
	[cfg_header]="Local settings for Docker Control Center. Not tracked by git."

	# --- make help ------------------------------------------------------------
	[help_title]="Docker Control Center"
	[help_usage]="— ${DCC_CMD} <target> · development only"
	[help_hint]="The product's own commands: make bundle, then ./dist/… help"

	[sec_setup]="Setup"
	[sec_tests]="Tests"
	[sec_quality]="Quality"
	[sec_release]="Release"
	[sec_overview]="Overview"
	[sec_stacks]="Stacks"
	[sec_containers]="Containers"
	[sec_volumes]="Volumes"
	[sec_cleanup]="Cleanup"
	[sec_engine]="Engine"

	[help_help]="Show this help"
	[help_lang]="Show the interface language and how to change it"
	[help_dash]="★ Dashboard: summary, stacks, volumes, images and disk"
	[help_dash-fast]="Dashboard with no sizes or disk — always ~1s"
	[help_status]="Executive summary: counted waste + real disk usage"
	[help_ps]="Containers grouped by stack, coloured by severity"
	[help_psa]="Alias of ps (the stack view already includes stopped ones)"
	[help_images]="Largest images (⌀ = no container uses it)"
	[help_volumes]="Volumes with real size and who uses them"
	[help_networks]="Defined networks"
	[help_stats]="Live usage (CPU/RAM) — Ctrl+C to quit"
	[help_ram]="RAM used by the engine (dockerd + containerd + proxies)"

	[help_stacks]="List stacks with their source directory"
	[help_stack-start]="Start ALL containers in a stack [stack]"
	[help_stack-stop]="Stop ALL containers in a stack [stack]"
	[help_stack-restart]="Restart ALL containers in a stack [stack]"
	[help_stack-logs]="Last 30 lines of EACH container in the stack [stack]"
	[help_stack-rm]="DANGER: delete a stack's containers (volumes are kept) [stack]"

	[help_logs]="Last 200 log lines [name]"
	[help_tail]="Live log [name]"
	[help_sh]="Shell inside the container [name]"
	[help_dex]="Typo-proof exec: ${DCC_CMD} dex <container> [cmd] · with flags use CMD='…'"
	[help_inspect]="Inspect a container as JSON [name]"
	[help_start]="Start a container [name]"
	[help_stop]="Stop a container [name]"
	[help_restart]="Restart a container [name]"
	[help_kill-all]="Stop ALL running containers"

	[help_volume-inspect]="Show a volume's details [volume]"
	[help_volume-tree]="List a volume's contents [volume]"
	[help_volume-backup]="Back a volume up to ./backups [volume]"
	[help_volume-backup-all]="Back ALL volumes up to ./backups"
	[help_volume-restore]="Restore a volume from ./backups [volume]"
	[help_volumes-orphan]="Volumes no container is using"

	[help_clean]="Safe cleanup: stopped containers, dangling images, unused networks"
	[help_clean-build]="Empty the build cache (frees a fair amount of disk)"
	[help_clean-hard]="DANGER: delete unused images and cache (volumes are kept)"
	[help_rm-image]="Delete an image [repo:tag]"

	[help_engine]="Native daemon status (the tray icon Desktop used to give you)"
	[help_ctx]="Show available contexts"

	# --- Makefile: pickers and operations -------------------------------------

	[pick_choose_container]="Choose a container:"
	[pick_none_container]="No containers available."
	[pick_choose_running]="Choose a running container:"
	[pick_none_running]="No running containers available."
	[pick_choose_volume]="Choose a volume:"
	[pick_none_volume]="No volumes available."
	[pick_choose_image]="Choose an image:"
	[pick_none_image]="No images available."
	[pick_choose_stack]="Choose a stack:"
	[pick_none_stack]="No stacks available."
	[op_no_containers]="No containers in %s."
	[op_nothing_running]="%s has nothing running."
	[op_started]="✓ %s started"
	[op_stopped]="✓ %s stopped"
	[op_restarted]="✓ %s restarted"
	[op_removed]="✓ %s deleted"
	[err_docker_down]="Cannot ask docker: the daemon is not responding."
	[op_nothing_up]="Nothing is running."

	[warn_rm_stack]="%s containers of %s are about to be deleted."
	[warn_rm_keep]="Volumes will NOT be touched. Neither will images."
	[warn_clean_hard]="This deletes every image not used by a container."
	[confirm_prompt]="Type YES to continue: "
	[confirm_word]="YES"

	[backup_failed_some]="%s of %s backups FAILED — do not rely on this directory."
	[clean_failed]="Cleanup did NOT finish: docker rejected at least one prune."
	[backup_done]="✓ Backups in %s"
	[backup_failed]="  (failed, moving on)"
	[restore_missing]="%s does not exist"
	[restore_done]="✓ Restored %s"
	[clean_done]="✓ Safe cleanup done"

	[engine_up]="● dockerd running"
	[engine_down]="○ dockerd stopped — start it with: sudo systemctl start docker"
	[engine_info]="· engine {{.ServerVersion}} · {{.Driver}} · {{.ContainersRunning}} running"
	[engine_boot_yes]="  starts on boot"
	[engine_boot_no]="  does NOT start on boot — sudo systemctl enable docker"

	# --- dashboard.sh ---------------------------------------------------------
	[warn_no_sizes]="Warning: without socket/curl/jq there are no sizes. Fast mode."
	[err_daemon]="The Docker daemon is not responding in context '%s'."
	[no_size_data]="No size data (fast mode)."

	[hdr_cpu]="CPU"
	[hdr_of_host]="of host"
	[hdr_engine]="engine"
	[hdr_ram_host]="host RAM"
	[hdr_stacks]="stacks"
	[hdr_containers_up]="containers up"
	[hdr_on_disk]="on disk"

	[trash_title]="IDENTIFIED WASTE"
	[trash_sub]="(counted from the API, not estimated by docker)"
	[trash_verified]="verified"
	[row_cache]="build cache"
	[row_cache_detail]="%s entries, 0 in use"
	[row_stopped]="stopped layer"
	[row_stopped_detail]="%s containers"
	[row_images]="unused images"
	[row_volumes]="orphan volumes"
	[row_volumes_detail]="%s unmounted"

	[alert_died]="died with an error"
	[alert_killed]="%s killed the hard way (137)"
	[alert_killed_sub]="SIGKILL / OOM / stop with timeout"
	[alert_old]="stopped weeks ago"

	[sec_stacks_title]="STACKS"
	[sec_stacks_sub]="— grouped by compose project"
	[stack_loose]="(loose)"
	[stack_up]="%s/%s up"
	[stack_stopped]="0/%s stopped"

	[sec_volumes_title]="VOLUMES"
	[vol_none]="(none)"
	[col_name]="NAME"
	[col_size]="SIZE"
	[col_usedby]="USED BY"
	[col_driver]="DRIVER"
	[col_scope]="SCOPE"
	[vol_orphan]="orphan"

	[sec_images_title]="IMAGES"
	[sec_images_sub]="— top 6 by size · ⌀ = no container uses it"

	[sec_disk_title]="DISK"
	[sec_disk_sub]="— real sizes; docker's 'reclaimable' is deliberately omitted"
	[disk_layers]="Image layers"
	[disk_containers]="Containers (rw)"
	[disk_volumes]="Volumes"
	[disk_cache]="Build cache"

	[st_created]="created"
	[st_restarting]="restarting"
	[footer_full]="${DCC_CMD} help · make clean · make clean-build · make stats"
	[footer_partial]="${DCC_CMD} dash for the full view"

	# --- dex.sh ---------------------------------------------------------------
	[dex_missing_name]="Missing container name."
	[dex_usage]="Usage: dex <container> [command...]"
	[dex_not_found]="No running container is called '%s'."
	[dex_none_at_all]="In fact, NOTHING is running. Bring something up with 'docker compose up -d'."
	[dex_none_running]="No containers are running right now."
	[dex_hint_up]="Bring something up with 'docker compose up -d' and come back."
	[dex_available]="AVAILABLE CONTAINERS"
	[dex_closest]="CLOSEST MATCH"
	[dex_service]="service"
	[dex_services]="services"
	[dex_no_stack]="no stack"
	[dex_stack]="stack:"
	[dex_opening]="Opening a shell in %s…"
	[sec_development]="Development"
	[help_deps]="Download the dev dependencies to vendor/ (see dependencies.txt)"
	[help_test-fast]="Run only the fast tests (skips the bundle build)"
	[help_run]="Run the built bundle: make run dash"
	[help_dev]="Rebuild and run in one go: make dev dash"
	[help_link]="Symlink the bundle as 'dcc' on your PATH"
	[help_lint]="Run shellcheck over every script"
	[help_test]="Run the unit tests (no docker needed) [T=name]"
	[help_check]="Lint + parse + tests: everything before a commit"
	[link_ok]="✓ dcc -> %s"
	[link_hint]="  Rebuild with 'make bundle' and dcc picks it up: it is a symlink, not a copy."
	[link_not_in_path]="⚠ %s is NOT in your PATH."
	[link_path_fix]="  Debian/Ubuntu only add it if the directory already existed at login. Fix it with:"
	[lint_ok]="✓ shellcheck clean"
	[lint_fail]="✗ shellcheck found problems"
	[lint_missing]="shellcheck is not installed. Install it with: brew install shellcheck"
	[check_ok]="✓ everything parses"
	[pick_no_tty]="No terminal available to show a menu. Pass the name, or pick one of these:"
	[restore_corrupt]="%s is corrupt (bad gzip). NOTHING has been deleted."
	[backup_failed_one]="Could not back up %s"
	[pick_unknown_container]="There is no container called '%s'."
	[pick_unknown_running]="There is no running container called '%s'."
	[pick_unknown_volume]="There is no volume called '%s'."
	[pick_unknown_image]="There is no image called '%s'."
	[pick_unknown_stack]="There is no stack called '%s'."
	[need_jq]="This command needs jq. Install it with: sudo apt install jq"
	[backup_dir_unwritable]="Cannot write to %s"
	[help_version]="Show the version and what was found on this system"
	[bundle_usage]="dcc <command> [name]"
	[bundle_more]="All commands:"
	[help_bundle]="Build dist/docker-control-center.sh: the whole tool in one file"
	[not_installed]="not installed"
	[help_usage_bundle]="— ${DCC_CMD} <command> [name]"
	[help_hint_bundle]="With no argument, each command opens a menu to choose from."
	[help_ram]="RAM used by the engine (dockerd + containerd + proxies)"
	[help_ctx]="Show available contexts"
	[help_stats]="Live usage (CPU/RAM) — Ctrl+C to quit"
	[help_coverage]="Line coverage of scripts/ with kcov"
	[help_fmt]="Format every script with shfmt"
	[help_hooks]="Install the pre-commit git hook"
	[need_kcov]="kcov is not installed. Install it with: brew install kcov"
	[need_shfmt]="shfmt is not installed. Install it with: brew install shfmt"
	[fmt_ok]="✓ formatted"
	[hooks_ok]="✓ hook installed at %s"
	[cov_title]="Line coverage"
	[cov_none]="No coverage data. Is kcov installed?"
)
